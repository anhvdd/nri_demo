generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["prismaSchemaFolder", "relationJoins", "fullTextSearch", "omitApi"]
    // "tracing"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}
